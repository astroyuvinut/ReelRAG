# ── Web ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1
pydantic>=2.7.0
httpx>=0.27.0

# ── Video / transcript ───────────────────────────────
yt-dlp>=2024.5.27
youtube-transcript-api>=1.0.0
# instaloader fills in instagram view counts that yt-dlp doesn't return
instaloader>=4.10

# ── Embeddings + Vector DB ───────────────────────────
# fastembed runs BGE-small on onnxruntime, no torch. keeps the image small.
fastembed>=0.3.0
chromadb>=0.5.0

# ── LLM orchestration ────────────────────────────────
langchain>=0.2.5
langchain-core>=0.2.9
langchain-community>=0.2.5
langchain-google-genai>=1.0.6
google-generativeai>=0.7.2

# ── Instagram audio transcription (no captions API) ──
# faster-whisper is lighter than openai-whisper (no PyTorch) and runs on CPU.
# Needs ffmpeg on PATH for audio extraction (winget install Gyan.FFmpeg).
# The app falls back to the video description if these aren't available.
faster-whisper>=1.0.0
